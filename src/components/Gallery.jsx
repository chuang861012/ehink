import React from "react";

export default ({ data }) => {
    // const tagsDict = {};
    // data.tags.forEach(tag => {
    //     const temp = tag.split(":");
    //     if (temp.length < 2) {
    //         if (!tagsDict.misc) tagsDict.misc = [temp[0]];
    //         else tagsDict.misc.push(temp[0]);
    //     } else {
    //         if (!tagsDict[temp[0]]) tagsDict[temp[0]] = [temp[1]];
    //         else tagsDict[temp[0]].push(temp[1]);
    //     }
    // });
    // const tagsArr = Object.entries(tagsDict).map(([k, v]) => {
    //     return { type: k, tags: v };
    // });
    // return (
    // );
};
